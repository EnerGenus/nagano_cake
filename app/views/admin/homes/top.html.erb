<h1>注文履歴一覧</h1>
<table class="table" >
  <thead>
    <tr>
      <th>購入日時</th>
      <th>購入者</th>
      <th>注文個数</th>
      <th>注文ステータス</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <!-- ordersコントローラーができたら注文詳細へのリンク埋め込む -->
        <td><%= order.created_at %></td>
        <td><%= order.user.name %></td>
        <td><%= order.items.count %></td>
        <td><%= order.status %></td>
      </tr>
    <% end %>
  </tbody>
</table>