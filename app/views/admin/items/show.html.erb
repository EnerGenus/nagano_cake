<div class="container-fluid position-relative">
  <div class="row justify-content-center">
    <div class="col-lg-10 col-md-8">
      <h5 class="d-inline-block p-2 m-4" style="background-color: #E0E0E0; color: #333;">商品詳細</h5>

      <div class="row d-flex align-items-left">
        <!-- 左側の画像 -->
        <div class="col-md-3 text-left">
        <%= image_tag @item.item_image, size: "300x300", class: "img-fluid rounded shadow-sm", alt: "商品画像" %>
        </div>

        <!-- 右側の商品情報 -->
        <div class="col-md-9">
          <table class="table-borderless">
            <tbody>
              <tr>
                <td class="col-4 text-left text-top">商品名</td>
                <td class="px-3 py-2"><%= @item.name %></td>
              </tr>
              <tr>
                <td class="col-4 text-left text-top">商品説明</td>
                <td class="px-3 py-2"><%= @item.description %></td>
              </tr>
              <tr>
                <td class="col-4 text-left text-top">ジャンル</td>
                <td class="px-3 py-2"><%= @item.genre.name %></td>
              </tr>
              <tr>
                <td class="col-4 text-left text-top">
                  税込価格<br>
                  <span class="text-muted">(税抜価格)</span>
                </td>
                <td class="px-3 py-2"><strong><%= @item.with_tax_price %></strong> 円 (<%= @item.price %> 円)</td>
              </tr>
              <tr>
                <td class="col-4 text-left text-top">販売ステータス</td>
                <td class="px-3 py-2">
                  <% if @item.is_active %>
                    <span class="text-success font-weight-bold">販売中</span>
                  <% else %>
                    <span class="text-secondary font-weight-bold">販売停止中</span>
                  <% end %>
                </td>
              </tr>
            </tbody>
          </table>
        </div>
      </div>

      <!-- 編集ボタン -->
      <div class="text-center mt-3">
        <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success px-4 py-2" %>
      </div>
    </div>
  </div>