<div>
  <%= image_tag @item.item_image, size: "300x300" %>
</div>
<div>
    <h2><strong><%= @item.name %></strong></h2>
    <p><%= @item.description %></p>
    <p>¥ <%= ((@item.price * 1.1).round)%>円(税込）</p>
    <% if @item.is_active == true %>
      <p class="text-success">販売中</p>
    <% else %>
      <p class="text-secondary">販売停止中</p>
    <% end %>
</div>

