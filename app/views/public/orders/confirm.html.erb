<div class="container">
  <div class="row">
    <h3 class="col-md-11">注文情報確認
    </h3>
  </div>
  <div class="row">
    <table class="col-md-7"> 
      <thead>
        <tr>
          <th>商品名</th>
          <th>単価(税込)</th>
          <th>数量</th>
          <th>小計</th>
        </tr>
      </thead>
      <tbody>
        <% @total = 0 %>
        <% @cart_items.each do |cart_item| %>
          <tr>
            <td>
              <%= image_tag cart_item.item.item_image, size: "50x50" %>
              <%= cart_item.item.name %>
            </td>
            <td><%= cart_item.item.with_tax_price %></td>
            <td>
              <%= cart_item.amount %>
            </td>
            <td><%= cart_item.subtotal %></td>
          </tr>
          <% @total += cart_item.subtotal %>
        <% end %>
      </tbody>
    </table>
    <table class="col-md-4">
      <thead>
        <tr>
          <th>送料</th>
          <th>商品合計</th>
          <th>請求金額</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>800</td>
          <td><%= @total %></td>
          <td><%= @total + 800 %></td>
        </tr>
      </tbody>
    </table>
  </div>
  <div class="row">
    <p><strong>支払方法</strong></p>
    <p><%= @order.payment_method %></p>
  </div>
  <div class="row">
    <p><strong>お届け先</strong></p>
    <p>〒<%= @order.postal_code %> <%= @order.address %></p><br>
    <p><%= @order.name %></p>
  </div>
  <div class="row">
    <%= form_with model: @order, url: orders_path, method: :post, local: true, scope: :order do |f| %>
      <%= f.hidden_field :payment_method, value: @order.payment_method %>
      <%= f.hidden_field :postal_code, value: @order.postal_code %>
      <%= f.hidden_field :address, value: @order.address %>
      <%= f.hidden_field :name, value: @order.name %>
      <%= f.text_field :shipping_cost, value: 800 %>
      <% @order.total_payment = @total.to_i + 800 %>
      <%= f.text_field :total_payment, value: @order.total_payment %>
      <%= f.hidden_field :status, value: 0 %>
      <%= f.text_field :customer_id, value: current_customer.id %>
      <%= f.submit "注文を確定する", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>
